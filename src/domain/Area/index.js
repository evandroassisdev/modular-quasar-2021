export default class Area {
  constructor (
    // id = null,
    name = null,
    frequency = null,
    what = null,
    roles = [],
    how = null,
    resource = null
  ) {
    // this.id = id
    this.name = name
    this.frequency = frequency
    this.what = what
    this.roles = roles
    this.how = how
    this.resource = resource
  }
}
